[project]
name = "illustrate-web"
version = "1.0.0"
description = "Local web interface for Goodsell's Illustrate biomolecular rendering tool"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0",
    "pillow>=12.0",
    "vtracer>=0.6",
]
